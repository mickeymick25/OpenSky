extends layout

block navigation
  container
    nav(class='navbar' role='navigation' aria-label='main navigation')
      div(class='navbar-brand')
        a(class='navbar-item' href='#')
          img(src='http://bulma.io/images/bulma-logo.png' alt='SkyGuard we make the sky safer' width='112' height='28')
        
        //- Burger menu for mobile
        button(class='button navbar-burger') 
          span
          span
          span
      
      //- Display menu item even on mobile
      div(class='navbar-menu is-active')
        div(class='navbar-start')
        div(class='navbar-end')
          a(class='navbar-item') S'inscrire
          a(class='navbar-item') Se connecter



block content
  //- p Welcome to #{title}
  section(class='hero')
    div(class='hero-body')
      div(class='container')
        h1(class='title') SkyGuard #{img_photograph}
        h2(class='subtitle') We make the sky safer with AI. #{img_description} 
        p #{img_url} 
        
  
block footer
  h1= "Footer"
  p footer
